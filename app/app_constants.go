package app

const frameRate = 60

const hapticFrameRate = frameRate * 2
const telemetryFrameRate = frameRate
const engineHapticFrameRate = frameRate / 2
const displayFramRate = frameRate / 4

// TODO: find a better place for this
const snapMultiplier = 160
